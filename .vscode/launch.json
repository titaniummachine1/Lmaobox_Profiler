{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Rapid dev (build + deploy + restart)",
			"type": "node-terminal",
			"request": "launch",
			"preLaunchTask": "Rapid dev (build + deploy + restart collector)"
		}
	]
}
